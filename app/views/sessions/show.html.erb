<!--this is the lobby where users can see high score, join the game, play the solo, create new game,  and logout

Solo game button; will redirect to games/:id,
Join game button; put join key, and then will redirect to games/:id (icebox)
Create game button; redirect to games/:new
Logout button; destroy session and redirect to session/index
 -->

<h1>Quiz Quest</h1>

<%= button_to "Logout", '/sessions' %>

<!-- Solo Game -->
  <%# 1. collection_select %>
    <%# 2. submit the category %>
  <%= form_for @session do |f|%>
      <%= collection_select :category, :category_id, Category.all, :id, :name %><br>
  <%= f.submit%>
   <% end %>

  <%#  3. redirect_to games/:id with the submitted category%>











 <%= form_tag games_path do %>
  <label>Solo Game:</label><br>
    <%= collection_select :category, :category_id, Category.all, :id, :name %><br>
  <input type="hidden" name="authenticity_token" value="<%= form_authenticity_token %>">
  <input type="submit" value="Submit Post">
<% end %>


<%# Create game %>
